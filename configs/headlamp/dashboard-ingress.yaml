apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: headlamp-dashboard-ingress
spec:
  entryPoints:
    - web
  routes:
    - kind: Rule
      match: Host(`headlamp.local`)
      services:
        - name: headlamp
          port: 80